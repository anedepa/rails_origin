<!-- <header class="navbar navbar-inverse"> -->
<header class="navbar navbar-fixed-top navbar-inverse head">
  <div class="container">

	<div class="logo">
	
  <canvas id="tutorial" width="150" height="30">
</canvas>
  <h2 class="logoc">試用アプリ</h2>
<%= link_to "bb", root_path, id: "logo",class:"logolink"%>
</div>


    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li><%= link_to "ユーザー", '#' %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">

              アカウント <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "ユーザー情報", current_user %></li>
              <li><%= link_to "設定", '#' %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "ログアウト", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "登録はこちら", new_user_url %></li>
          <li><%= link_to "ログイン", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>

<script>
  var startPos = 0,winScrollTop = 0;
$(window).on('scroll',function(){
    winScrollTop = $(this).scrollTop();
    if (winScrollTop >= startPos) {
        $('.head').addClass('hide');
    } else {
        $('.head').removeClass('hide');
    }
    startPos = winScrollTop;
});
$(window).on('beforeunload', function() {
        $('.head').removeClass('hide');
    });
  </script>
